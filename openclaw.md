# openclaw

The OpenClaw gateway is the local OpenClaw agent server. It receives messages over WebSocket, runs the full OpenClaw agent/tool/skill loop, calls LLMs through the local proxy, and streams responses back.
